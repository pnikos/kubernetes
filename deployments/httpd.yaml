apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nikos-httpd
  name: nikos-httpd
spec:
  containers:
  - image: httpd
    name: nikos-httpd
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
